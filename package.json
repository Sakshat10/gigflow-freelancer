{
  "name": "gigflow-freelancer",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd frontend && bun run dev",
    "build": "cd frontend && bun run build",
    "preview": "cd frontend && bun run preview",
    "lint": "cd frontend && bun run lint",
    "clean": "cd frontend && rm -rf dist"
  },
  "devDependencies": {
    "prisma": "^7.2.0",
    "typescript": "^5.5.3"
  },
  "dependencies": {
    "@prisma/client": "^7.2.0"
  }
}